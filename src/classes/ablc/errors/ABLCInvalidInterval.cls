class classes.ablc.errors.ABLCInvalidInterval inherits Progress.Lang.AppError:
    constructor ABLCInvalidInterval():
        addMessage("the interval must be greater then 0", -4501).
    end constructor.
end class.
